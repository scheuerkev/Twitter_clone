extends ../layout

block content
    .d-flex.flex-row.content.mx-auto
        .profile-container
            include ../includes/profile
        .flex-fill.d-flex.flex-column
            if tweets
                each tweet in tweets
                    .card.tweet-element
                        .card-body= tweet.content