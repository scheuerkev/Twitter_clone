.profile-container.me-3.bg-white.d-flex.flex-column
    form.d-flex.flex-row.justify-content-center.align-items-center(action="/users/update/image" method="post" enctype="multipart/form-data")
        input(type="file" name="avatar" class="d-none")
        img(src="/images/zombiiie.png" class="img-fluid")
    .d-flex.flex-row.justify-content-center.align-items-center.my-3
        p= `${currentUser.username} (${currentUser.local.email})`
    .text-center.d-flex.flex-row.justify-content-center.align-items-center.my-3
        .flex-fill
            p
                strong Tweet
                br
                span(class="text-primary")= tweets && tweets.length ? tweets.length : "-"
        .flex-fill
            p
                strong Following
                br
                span(class="text-primary") -